const routes = {
    home: '/',
    profile: '/profile/:nickname',
    upload: '/upload',
    follow: '/follow',
    live: '/live',
    videoViewer: '/profile/:nickname/video/:videoId',
}

export default routes;